# Copy this file to `.env` and fill in a real key for local development.
# `.env` is git-ignored and must never be committed.
# On Vercel, set this under Project -> Settings -> Environment Variables instead.
OPENAI_API_KEY=sk-your-key-here